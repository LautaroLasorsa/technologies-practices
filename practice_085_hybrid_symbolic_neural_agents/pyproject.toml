[project]
name = "practice-085-hybrid-symbolic-neural-agents"
version = "0.1.0"
description = "Hybrid Symbolic-Neural Agents — LLM orchestrates, CP-SAT solves, LLM explains"
requires-python = ">=3.11"
dependencies = [
    "ortools>=9.10",
    "pydantic>=2.6",
    "litellm>=1.50",
    "instructor>=1.6",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
