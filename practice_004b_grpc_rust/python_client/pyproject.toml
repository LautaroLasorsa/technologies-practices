[project]
name = "python-client"
version = "0.1.0"
description = "Python gRPC client for the Rust Inventory service"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "grpcio>=1.78.0",
    "grpcio-tools>=1.78.0",
]
