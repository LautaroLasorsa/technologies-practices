[project]
name = "order-service"
version = "0.1.0"
description = "Order service - accepts HTTP orders and triggers sagas"
requires-python = ">=3.12"
dependencies = [
    "aiokafka>=0.12.0",
    "fastapi>=0.115.0",
    "uvicorn>=0.34.0",
]
