[project]
name = "orchestrator"
version = "0.1.0"
description = "Saga orchestrator - central coordinator for distributed transactions"
requires-python = ">=3.12"
dependencies = [
    "aiokafka>=0.12.0",
]
