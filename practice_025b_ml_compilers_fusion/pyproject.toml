[project]
name = "practice-025b-ml-compilers-fusion"
version = "0.1.0"
description = "Operator fusion and graph rewrites with torch.fx"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
