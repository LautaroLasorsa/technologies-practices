[project]
name = "practice-041b-conic-programming"
version = "0.1.0"
description = "Conic Programming — SOCP & SDP with CVXPY"
requires-python = ">=3.12"
dependencies = [
    "cvxpy>=1.5",
    "numpy>=1.26",
    "scipy>=1.12",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
