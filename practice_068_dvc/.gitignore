# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/

# DVC local remote storage (simulated "cloud")
/tmp_remote_storage/

# DVC-tracked data outputs are managed by DVC itself.
# When you run `dvc add <file>`, DVC creates .gitignore entries automatically.
# Do NOT manually add data/models paths here -- let DVC handle it so you can
# observe the .gitignore management as a learning exercise.

# DVC internal cache (lives in .dvc/cache, covered by .dvc/.gitignore)
# Explicitly ignore in case of stray copies
.dvc/cache/
.dvc/tmp/

# Params and metrics are TRACKED in git (small files), so we do NOT ignore:
# !params.yaml
# !metrics/*.json  (handled by dvc.yaml cache: false)

# OS
*.swp
*~
Thumbs.db
.DS_Store
