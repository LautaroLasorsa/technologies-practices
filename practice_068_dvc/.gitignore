# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/

# DVC local remote storage (simulated "cloud")
/tmp_remote_storage/

# DVC-tracked data outputs (the actual large files; .dvc pointers are committed)
/data/raw/*.csv
/data/prepared/
/models/
/metrics/

# DVC internal cache (lives in .dvc/cache, covered by .dvc/.gitignore)
# Explicitly ignore in case of stray copies
.dvc/cache/
.dvc/tmp/

# Params and metrics are TRACKED in git (small files), so we do NOT ignore:
# !params.yaml
# !metrics/*.json  (handled by dvc.yaml cache: false)

# OS
*.swp
*~
Thumbs.db
.DS_Store
