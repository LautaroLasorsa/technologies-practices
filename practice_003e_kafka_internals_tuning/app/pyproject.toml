[project]
name = "kafka-internals-tuning"
version = "0.1.0"
description = "Practice 003e: Kafka Internals & Performance Tuning"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "confluent-kafka>=2.6.0",
    "confluent-kafka-stubs>=0.0.3",
]

[dependency-groups]
dev = [
    "basedpyright>=1.38.0",
]
