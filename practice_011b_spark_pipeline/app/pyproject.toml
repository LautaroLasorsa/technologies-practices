[project]
name = "spark-pipeline"
version = "0.1.0"
description = "Retail analytics ETL pipeline with PySpark"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyspark>=3.5.0,<4.0.0",
]
