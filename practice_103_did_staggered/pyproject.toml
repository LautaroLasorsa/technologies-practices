[project]
name = "practice-103-did-staggered"
version = "0.1.0"
description = "Difference-in-Differences & the Staggered Adoption Problem"
requires-python = ">=3.11"
dependencies = [
    "xy==0.0.7",
    "jupyterlab>=4.2",
    "nbstripout>=0.7",
    "pandas>=2.2",
    "numpy>=1.26",
    "pyfixest>=0.60",
    "differences>=0.3",
]

[tool.uv]
package = false
