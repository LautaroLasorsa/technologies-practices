[project]
name = "k8s-backend"
version = "0.1.0"
description = "Practice 006b: Backend API for Kubernetes services, scaling & ingress"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.34.0",
]
