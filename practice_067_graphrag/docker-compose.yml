services:
  ollama:
    image: ollama/ollama:latest
    container_name: graphrag_ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    deploy:
      resources:
        reservations:
          memory: 8G

volumes:
  ollama_data:
