"""Entry point for `python -m app`.

The main learning module is app/queries.py. Run it directly:
  uv run python -m app.queries
"""

print("Run the queries module directly:")
print("  uv run python -m app.queries")
print()
print("Or run the seed script:")
print("  uv run python -m app.seed")
