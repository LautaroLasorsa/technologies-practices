[project]
name = "practice-078-distributed-rate-limiting"
version = "0.1.0"
description = "Distributed Rate Limiting: Token Bucket, Sliding Window & Redis Lua"
requires-python = ">=3.12"
dependencies = [
    "redis>=5.0",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "httpx>=0.27",
    "matplotlib>=3.7",
]
